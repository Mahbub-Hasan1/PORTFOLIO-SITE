import React from 'react';

const LoginPage = () => {
    const login = 3333333333;
    return (
        <div>
            <h1>Login Page {login}</h1>
        </div>
    );
};

export default LoginPage;
